<%= image_tag(@home.picture_url, :height => 800) if @home.picture.present? %>
<%= RedCloth.new(@home.title).to_html.html_safe %>

<%= RedCloth.new(@home.description).to_html.html_safe %>


<p>
<%= link_to 'Edit', edit_home_path(@home) %> |
<%= link_to 'Back', homes_path %>
</p>
