<%= form_for(@page_subheading) do |f| %>
  <% if @page_subheading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_subheading.errors.count, "error") %> prohibited this page_subheading from being saved:</h2>

      <ul>
      <% @page_subheading.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% if params[:page_id].to_i == 1%>
    <p>This subheading belongs to the Home Page</p>
  <%end %>

   <% if params[:page_id].to_i == 2%>
    <p>This subheading belongs to the Expo Page</p>
  <%end %>

   <% if params[:page_id].to_i == 3%>
    <p>This subheading belongs to Industry Page</p>
  <%end %>
  
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
