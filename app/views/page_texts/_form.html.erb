<%= form_for(@page_text) do |f| %>
  <% if @page_text.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_text.errors.count, "error") %> prohibited this page_text from being saved:</h2>

      <ul>
      <% @page_text.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= Page.find(params[:page_id]).name %>

  <% if params[:page_id].to_i == 1%>
    <p>This text belongs to the Home Page</p>
  <%end %>

   <% if params[:page_id].to_i == 2%>
    <p>This text belongs to the Expo Page</p>
  <%end %>

   <% if params[:page_id].to_i == 3%>
    <p>This text belongs to Industry Page</p>
  <%end %>

  <%= params[:page_id] %>

  <div class="field">
    <%= hidden_field(:page, :page_id, :value => params[:page_id]) %> 
  </div>
 
  <div class="field">
    <%= f.text_area :description %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>


<% end %>
