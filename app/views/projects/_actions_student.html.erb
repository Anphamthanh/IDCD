<% options = [] %>

<%# options.push({ 
  label: "Details",  
  controller: "projects",
  action: "show",
  id: project.id,
  method: "get",
  confirm: false}) 
%>

<% if options.length > 0 %>
  <%= render :partial => "shared/dropdown_actions", :locals => {options: options.reverse!} %>
<% end %>

