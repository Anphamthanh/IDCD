<%= form_for(@page_picture) do |f| %>
  <% if @page_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_picture.errors.count, "error") %> prohibited this page_picture from being saved:</h2>

      <ul>
      <% @page_picture.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= hidden_field(:page, :page_id, :value => params[:page_id]) %> 
  </div>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :picture %><br />
    <%= f.file_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
