<h1>Listing headings</h1>

<table>
  <tr>
    <th>Title</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @headings.each do |heading| %>
  <tr>
    <td><%= heading.title %></td>
    <td><%= link_to 'Show', page_heading_path(@page, heading) %></td>
    <td><%= link_to 'Edit', edit_page_heading_path(@page,heading) %></td>
    <td><%= link_to 'Destroy',page_heading_path(@page,heading), method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Heading', new_page_heading_path %>
