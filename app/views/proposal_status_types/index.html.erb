<h1>Listing proposal_status_types</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @proposal_status_types.each do |proposal_status_type| %>
  <tr>
    <td><%= proposal_status_type.name %></td>
    <td><%= link_to 'Show', proposal_status_type %></td>
    <td><%= link_to 'Edit', edit_proposal_status_type_path(proposal_status_type) %></td>
    <td><%= link_to 'Destroy', proposal_status_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Proposal status type', new_proposal_status_type_path %>
