<%= form_for(@subheading) do |f| %>
  <% if @subheading.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@subheading.errors.count, "error") %> prohibited this subheading from being saved:</h2>

      <ul>
      <% @subheading.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
