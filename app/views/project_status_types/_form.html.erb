<%= form_for(@project_status_type) do |f| %>
  <% if @project_status_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project_status_type.errors.count, "error") %> prohibited this project_status from being saved:</h2>

      <ul>
      <% @project_status_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
